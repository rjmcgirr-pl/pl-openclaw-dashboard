name: Deploy Task Board

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
  workflow_dispatch:  # Allow manual trigger

jobs:
  # Stage 1: Validate code
  validate:
    name: Validate Code
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./worker
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './worker/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: TypeScript Type Check
        run: npx tsc --noEmit

      - name: Lint Check
        run: npm run lint || echo "No lint script, skipping"

  # Stage 2: Deploy to Staging
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: validate
    defaults:
      run:
        working-directory: ./worker
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './worker/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Inject Staging Secrets
        env:
          DB_ID: ${{ secrets.D1_DATABASE_ID }}
          PASSWORD: ${{ secrets.DASHBOARD_PASSWORD }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
        run: |
          node -e "
            const fs = require('fs');
            const content = fs.readFileSync('wrangler.toml', 'utf8');
            const dbId = process.env.DB_ID;
            const password = process.env.PASSWORD;
            const googleClientId = process.env.GOOGLE_CLIENT_ID || 'placeholder';
            const googleClientSecret = process.env.GOOGLE_CLIENT_SECRET || 'placeholder';
            const sessionSecret = process.env.SESSION_SECRET || 'placeholder';
            const updated = content
              .replace(/your-database-id-here/g, dbId)
              .replace(/your-password-here/g, password)
              .replace(/<your-session-kv-namespace-id>/g, '${{ secrets.SESSION_KV_NAMESPACE_ID }}')
              .replace(/placeholder/g, googleClientId);
            fs.writeFileSync('wrangler.toml', updated);
          "

      - name: Deploy Worker to Staging
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: ./worker
          command: deploy --env staging --secret GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }} --secret SESSION_SECRET=${{ secrets.SESSION_SECRET }}

      - name: Run D1 Migrations on Staging
        continue-on-error: true
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: ./worker
          command: d1 execute openclaw-taskboard-db --file=../schema.sql

  # Stage 3: Smoke Test Staging
  smoke-test-staging:
    name: Smoke Test Staging
    runs-on: ubuntu-latest
    needs: deploy-staging
    
    steps:
      - name: Wait for deployment
        run: sleep 10

      - name: Test Staging API
        run: |
          echo "Testing staging API..."
          STAGING_URL="https://taskboard-api-staging.rei-workers.workers.dev"
          
          # Test 1: Auth endpoints respond
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "$STAGING_URL/auth/google" || echo "000")
          if [ "$HTTP_CODE" = "302" ]; then
            echo "✅ OAuth endpoint redirects (correct)"
          else
            echo "❌ Expected 302, got $HTTP_CODE"
            exit 1
          fi
          
          # Test 2: Protected routes require auth
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "$STAGING_URL/tasks" || echo "000")
          if [ "$HTTP_CODE" = "401" ]; then
            echo "✅ Protected routes require auth (correct)"
          else
            echo "❌ Expected 401, got $HTTP_CODE"
            exit 1
          fi
          
          echo "✅ Staging smoke tests passed"

  # Stage 4: Deploy to Production
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: smoke-test-staging
    defaults:
      run:
        working-directory: ./worker
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './worker/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Inject Production Secrets
        env:
          DB_ID: ${{ secrets.D1_DATABASE_ID }}
          PASSWORD: ${{ secrets.DASHBOARD_PASSWORD }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
        run: |
          node -e "
            const fs = require('fs');
            const content = fs.readFileSync('wrangler.toml', 'utf8');
            const dbId = process.env.DB_ID;
            const password = process.env.PASSWORD;
            const googleClientId = process.env.GOOGLE_CLIENT_ID || 'placeholder';
            const googleClientSecret = process.env.GOOGLE_CLIENT_SECRET || 'placeholder';
            const sessionSecret = process.env.SESSION_SECRET || 'placeholder';
            const updated = content
              .replace(/your-database-id-here/g, dbId)
              .replace(/your-password-here/g, password)
              .replace(/<your-session-kv-namespace-id>/g, '${{ secrets.SESSION_KV_NAMESPACE_ID }}')
              .replace(/placeholder/g, googleClientId);
            fs.writeFileSync('wrangler.toml', updated);
          "

      - name: Deploy Worker to Production
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: ./worker
          command: deploy --env production --secret GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }} --secret SESSION_SECRET=${{ secrets.SESSION_SECRET }}

      - name: Run D1 Migrations on Production
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: ./worker
          command: d1 execute openclaw-taskboard-db --file=../schema-cron-only.sql --env production

      - name: Deploy Pages to Production
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: .
          command: pages deploy public --project-name=taskboard

      - name: Output Deployed URLs
        run: |
          echo "✅ Production deployed"
          echo "Worker: https://openclaw.propertyllama.com"
          echo "Pages: Check Cloudflare dashboard"

  # Stage 5: Smoke Test Production
  smoke-test-production:
    name: Smoke Test Production
    runs-on: ubuntu-latest
    needs: deploy-production
    
    steps:
      - name: Wait for deployment
        run: sleep 30

      - name: Test Production API
        run: |
          echo "Testing production API..."
          PROD_URL="https://taskboard-api.rei-workers.workers.dev"
          
          # Test 1: API responds (with retries)
          for i in 1 2 3; do
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "$PROD_URL/auth/google" || echo "000")
            if [ "$HTTP_CODE" = "302" ]; then
              echo "✅ OAuth endpoint responding (HTTP $HTTP_CODE)"
              break
            elif [ "$HTTP_CODE" != "000" ] && [ "$HTTP_CODE" != "502" ] && [ "$HTTP_CODE" != "503" ]; then
              echo "✅ API responding (HTTP $HTTP_CODE)"
              break
            elif [ "$i" = "3" ]; then
              echo "⚠️ API not responding after retries (HTTP $HTTP_CODE)"
            else
              echo "Retry $i... (got HTTP $HTTP_CODE)"
              sleep 10
            fi
          done
          
          # Test 2: Protected routes require auth
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "$PROD_URL/tasks" || echo "000")
          if [ "$HTTP_CODE" = "401" ]; then
            echo "✅ Protected routes require auth"
          else
            echo "⚠️ Expected 401, got $HTTP_CODE"
          fi
          
          echo "✅ Production smoke tests completed"
